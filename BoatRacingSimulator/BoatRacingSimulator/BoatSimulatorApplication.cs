namespace BoatRacingSimulator
{
    using Core;

    public class BoatSimulatorApplication
    {
        public static void Main()
        {
            var engine = new Engine();
            engine.Run();
        }
    }
}